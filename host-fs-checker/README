For K8S:
If scripts/fs.sh has a loop within and Dockerfile has an entrypoint, then we dont need to specify the loop in deploy.yaml
If scripts/fs.sh just does the work once and exits, then a loop has to be placed in deploy.yaml
